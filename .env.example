# Asset Portal

Copy values into the service-specific env files before running locally.

## Shared guidance
- Set real Keycloak values
- Replace placeholder database credentials
- Do not commit real secrets
