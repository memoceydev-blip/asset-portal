apiVersion: v1
kind: ConfigMap
metadata:
  name: asset-frontend-config
  namespace: asset-portal
data:
  config.js: |
    window.APP_CONFIG = {
      API_BASE_URL: "http://asset-backend:8000",
      KEYCLOAK_URL: "https://keycloak.example.com",
      KEYCLOAK_REALM: "master",
      KEYCLOAK_CLIENT_ID: "asset-frontend"
    };
